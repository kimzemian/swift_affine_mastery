from setuptools import setup, find_packages

setup(
    name='swift_affine_mastery',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)